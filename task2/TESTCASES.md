# Тест-кейсы для метода POST {{baseUrl}}/api/1/item (Сохранить объявление)

## Предусловия

- **Эндпоинт:** `{{baseUrl}}/api/1/item`
- **Метод:** `POST`
- **Заголовки:** `Content-Type: application/json`


**Схема 1:** Схема тела POST запроса на {{baseUrl}}/api/1/item.

**Схема 2:** Схема успешного ответа 200 OK для POST {{baseUrl}}/api/1/item.

**Схема 3:** Схема ответа об ошибке 400 Bad Request.

**Схема 1:**

``` 
{
  "sellerID": "<integer>",
  "name": "<string>",
  "price": "<integer>",
  "statistics": {
    "likes": "<integer>",
    "viewCount": "<integer>",
    "contacts": "<integer>"
  }
}
```

**Схема 2:**

``` 
{
  "id": "<string>",
  "sellerId": "<integer>",
  "name": "<string>",
  "price": "<integer>",
  "statistics": {
    "likes": "<integer>",
    "viewCount": "<integer>",
    "contacts": "<integer>"
  },
  "createdAt": "<string>"
}
```

**Схема 3:**

``` 
{
  "result": {
    "messages": {
      "culpa_b92": "<string>",
      "enim_24f": "<string>",
      "mollit_aa": "<string>"
    },
    "message": "<string>"
  },
  "status": "<string>"
}
```

## 1. Позитивные тесты

| №     | Описание                                                                                                                                                              | Тело запроса (JSON)                                                                                                              | Ожидаемый результат                                                                                                                                                                                                                                                                                                                              |
|:------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------|:---------------------------------------------------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1** | Сформировать HTTP-запрос в соответствии со **схемой 1** с валидными данными для каждого поля                                                                          | ```{ "sellerID": 111303, "name": "Объявление", "price": 100, "statistics": { "likes": 10, "viewCount": 11, "contacts": 12 } }``` | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 2**. Значения полей тела ответа соответствуют значениям полей из тела запроса. Тело ответа также содержит поля id и createdAt со значениями формата uuid и даты создания объявления соответственно                                                                                                                                                                               |
| **2** | Сформировать HTTP-запрос в соответствии со **схемой 1** с полями, в которых допустимы нулевые значения (`price`, блок `statistics`: `likes`, `viewCount`, `contacts`) | ```{ "sellerID": 111303, "name": "Объявление", "price": 0, "statistics": { "likes": 0, "viewCount": 0, "contacts": 0 } }```      | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 2** с нулевыми значениями в полях `price` и блока `statistics`. Значения полей тела ответа соответствуют значениям полей из тела запроса. Тело ответа также содержит поля id и createdAt со значениями формата uuid и даты создания объявления соответственно |

## 2. Негативные тесты

| №     | Описание                                                                                                                                                               | Тело запроса (JSON)                                                                                                                                              | Ожидаемое тело ответа (пример)                                                                                                                                                                                                                                                                 |
|:------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **3** | Сформировать HTTP-запрос в соответствии со **схемой 1** с отсутствием одного или нескольких обязательных полей                                                         | ```{ "name": "Отсутствие sellerId и price", "statistics": { "likes": 10, "viewCount": 56, "contacts": 4 } } ```                                                  | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки/ок, блок `message` - сообщения об ошибках для каждого отсутствующего обязательного поля. |
| **4** | Сформировать HTTP-запрос в соответствии со **схемой 1** со значением `null` одного или нескольких обязательных полей                                                   | ```{ "sellerID": 111303, "name": null, "price": 100, "statistics": { "likes": 10, "viewCount": 56, "contacts": 4 } } ```                                         | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки/ок, блок `message` - сообщения об ошибках, указывающие, что поле не может быть `null`.   |
| **5** | Сформировать HTTP-запрос в соответствии со **схемой 1**, в которой значение одного или нескольких полей имеет тип, не соответствующий спецификации                     | ```{ "sellerID": "111303", "name": "Неверные типы sellerId и price", "price": "100", "statistics": { "likes": 12, "viewCount": 6, "contacts": 5 } } ```          | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки/ок, блок `message` - сообщения об ошибках с указанием поля и ожидаемого типа данных.     |
| **6** | Сформировать HTTP-запрос в соответствии со **схемой 1**, в которой значение одного или нескольких числовых полей отрицательное                                         | ```{ "sellerID": 111303, "name": "Отрицательные значения price и viewCount", "price": -500, "statistics": { "likes": 89, "viewCount": -3, "contacts": 10 } } ``` | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки/ок, блок `message` - сообщения об ошибках неотрицательности каждого числового поля.      |
| **7** | Сформировать HTTP-запрос в соответствии со **схемой 1** с пустым значением поля `name` и со значением, состоящим из пробелов                                           | ```{ "sellerID": 111303, "name": "       ", "price": 100, "statistics": { "likes": 20, "viewCount": 45, "contacts": 5 } } ```                                    | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке пустого значения поля `name`.                      |
| **8** | Сформировать HTTP-запрос в соответствии со **схемой 1** со значением поля `name`, состоящим из спецсимволов                                                            | ```{ "sellerID": 111303, "name": "@&#", "price": 100, "statistics": { "likes": 20, "viewCount": 45, "contacts": 5 } } ```                                        | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке недопустимых сиволов в поле `name`.                |

## 3. Граничные тесты
| №      | Описание                                                                                       | Тело запроса (JSON)                                                                                                                                                       | Ожидаемое тело ответа (пример)                                                                                                                                                                                                                                             |
|:-------|:-----------------------------------------------------------------------------------------------|:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **9**  | Сформировать HTTP-запрос в соответствии со **схемой 1** со значением поля `sellerId = 111111`  | ```{ "sellerID": 111111, "name": "Нижнее допустимое значение sellerId", "price": 100, "statistics": { "likes": 10, "viewCount": 11, "contacts": 12 } }```                 | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 2**. Значения полей тела ответа соответствуют значениям полей из тела запроса.                                                                                                          |
| **10** | Сформировать HTTP-запрос в соответствии со **схемой 1** со значением поля `sellerId = 999999`  | ```{ "sellerID": 999999, "name": "Верхнее допустимое значение sellerId", "price": 100, "statistics": { "likes": 10, "viewCount": 11, "contacts": 12 } }```                | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 2**. Значения полей тела ответа соответствуют значениям полей из тела запроса.                                                                                                          |
| **11** | Сформировать HTTP-запрос в соответствии со **схемой 1** со значением поля `sellerId = 111110`  | ```{ "sellerID": 111110, "name": "Недопустимое значение sellerId со стороны нижнего", "price": 100, "statistics": { "likes": 10, "viewCount": 11, "contacts": 12 } }```   | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке допустимого значения sellerId. |
| **12** | Сформировать HTTP-запрос в соответствии со **схемой 1** со значением поля `sellerId = 1000000` | ```{ "sellerID": 1000000, "name": "Недопустимое значение sellerId со стороны верхнего", "price": 100, "statistics": { "likes": 10, "viewCount": 11, "contacts": 12 } }``` | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке допустимого значения sellerId. |


# Тест-кейсы для метода GET {{baseUrl}}/api/1/item/:id (Получить объявление по идентификатору)

## Предусловия

1. При помощи метода POST {{baseUrl}}/api/1/item создать объявление
2. Из тела ответа взять значение поля `id`
3. Использовать значение в path-параметре

- **Эндпоинт:** `{{baseUrl}}/api/1/item/:id`
- **Метод:** `GET`
- **Заголовки:** `Accept: application/json`

**Схема 1:** Схема успешного ответа 200 OK для GET {{baseUrl}}/api/1/item/:id.

**Схема 2:** Схема ответа об ошибке 404 Not Found.

**Схема 3:** Схема ответа об ошибке 400 Bad Request.

**Схема 1:**

``` 
[
  {
    "id": "<string>",
    "sellerId": "<integer>",
    "name": "<string>",
    "price": "<integer>",
    "statistics": {
      "likes": "<integer>",
      "viewCount": "<integer>",
      "contacts": "<integer>"
    },
    "createdAt": "<string>"
  },
  {
    "id": "<string>",
    "sellerId": "<integer>",
    "name": "<string>",
    "price": "<integer>",
    "statistics": {
      "likes": "<integer>",
      "viewCount": "<integer>",
      "contacts": "<integer>"
    },
    "createdAt": "<string>"
  }
]
```

**Схема 2:**

``` 
{
  "result": "laborum",
  "status": "cillum enim eiusmod"
}
```

**Схема 3:**

``` 
{
  "result": {
    "messages": {
      "culpa_b92": "<string>",
      "enim_24f": "<string>",
      "mollit_aa": "<string>"
    },
    "message": "<string>"
  },
  "status": "<string>"
}
```



## 1. Позитивные тесты

| №     | Описание                                                                             | Параметры запроса                                      | Ожидаемый результат                                                                                                                                                                     |
|:------|:-------------------------------------------------------------------------------------|:-------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1** | Сформировать GET-запрос с path-параметром - корректным и существующим в системе UUID | id = "id_из_созданного_объявления" (существующий uuid) | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 1**. Значения полей тела ответа соответствуют значениям полей из тела запроса созданного объявления. |

## 2. Негативные тесты
| №     | Описание                                                                                           | Параметры запроса                                                 | Ожидаемый результат                                                                                                                                                                                                                                                      |
|:------|:---------------------------------------------------------------------------------------------------|:------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **2** | Сформировать GET-запрос с path-параметром - валидным по формату, но не существующим в системе UUID | id = "1d0321d3-dd36-4baf-98f1-fdcd37266db5" (несуществующий uuid) | Сервер возвращает HTTP статус-код `404 Not Found`. Структура ответа соответствует **схеме 2**. Поле `status` содержит возвращаемый код-статус, поле `result` - сообщение об ошибке несуществования объявления с данным UUID.                                             |
| **3** | Сформировать GET-запрос с path-параметром - не соответствующим формату UUID                        | id = "123abc" (не соответствует формату uuid)                     | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке недопустимого значения `id`. |


# Тест-кейсы для метода GET {{baseUrl}}/api/1/statistic/:id (Получить статистику по объявлению)

## Предусловия

1. При помощи метода POST {{baseUrl}}/api/1/item создать объявление
2. Из тела ответа взять значение поля `id`
3. Использовать значение в path-параметре

- **Эндпоинт:** `{{baseUrl}}/api/1/statistic/:id`
- **Метод:** `GET`
- **Заголовки:** `Accept: application/json`

**Схема 1:** Схема успешного ответа 200 OK для GET {{baseUrl}}/api/1/statistic/:id.

**Схема 2:** Схема ответа об ошибке 404 Not Found.

**Схема 3:** Схема ответа об ошибке 400 Bad Request.

**Схема 1:**

``` 
[
  {
    "likes": "<integer>",
    "viewCount": "<integer>",
    "contacts": "<integer>"
  },
  {
    "likes": "<integer>",
    "viewCount": "<integer>",
    "contacts": "<integer>"
  }
]
```

**Схема 2:**

``` 
{
  "result": "laborum",
  "status": "cillum enim eiusmod"
}
```

**Схема 3:**

``` 
{
  "result": {
    "messages": {
      "culpa_b92": "<string>",
      "enim_24f": "<string>",
      "mollit_aa": "<string>"
    },
    "message": "<string>"
  },
  "status": "<string>"
}
```

## 1. Позитивные тесты

| №     | Описание                                                                             | Параметры запроса                                      | Ожидаемый результат                                                                                                                                                                     |
|:------|:-------------------------------------------------------------------------------------|:-------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1** | Сформировать GET-запрос с path-параметром - корректным и существующим в системе UUID | id = "id_из_созданного_объявления" (существующий uuid) | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 1**. Значения полей тела ответа соответствуют значениям полей из тела запроса созданного объявления. |

## 2. Негативные тесты
| №     | Описание                                                                                           | Параметры запроса                                                 | Ожидаемый результат                                                                                                                                                                                                                                                      |
|:------|:---------------------------------------------------------------------------------------------------|:------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **2** | Сформировать GET-запрос с path-параметром - валидным по формату, но не существующим в системе UUID | id = "1d0321d3-dd36-4baf-98f1-fdcd37266db5" (несуществующий uuid) | Сервер возвращает HTTP статус-код `404 Not Found`. Структура ответа соответствует **схеме 2**. Поле `status` содержит возвращаемый код-статус, поле `result` - сообщение об ошибке несуществования объявления с данным UUID.                                             |
| **3** | Сформировать GET-запрос с path-параметром - не соответствующим формату UUID                        | id = "123abc" (не соответствует формату uuid)                     | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке недопустимого значения `id`. |


# Тест-кейсы для метода GET {{baseUrl}}/api/1/:sellerID/item (Получить все объявления пользователя)

## Предусловия

1. При помощи метода POST {{baseUrl}}/api/1/item создать объявление
2. Из тела запроса взять значение поля `sellerId`
3. Использовать значение в path-параметре

- **Эндпоинт:** `{{baseUrl}}/api/1/:sellerID/item`
- **Метод:** `GET`
- **Заголовки:** `Accept: application/json`

**Схема 1:** Схема успешного ответа 200 OK для GET {{baseUrl}}/api/1/:sellerID/item.

**Схема 2:** Схема ответа об ошибке 400 Bad Request.

**Схема 1:**

``` 
[
  {
    "id": "<string>",
    "sellerId": "<integer>",
    "name": "<string>",
    "price": "<integer>",
    "statistics": {
      "likes": "<integer>",
      "viewCount": "<integer>",
      "contacts": "<integer>"
    },
    "createdAt": "<string>"
  },
  {
    "id": "<string>",
    "sellerId": "<integer>",
    "name": "<string>",
    "price": "<integer>",
    "statistics": {
      "likes": "<integer>",
      "viewCount": "<integer>",
      "contacts": "<integer>"
    },
    "createdAt": "<string>"
  }
]
```

**Схема 2:**

``` 
{
  "result": {
    "messages": {
      "culpa_b92": "<string>",
      "enim_24f": "<string>",
      "mollit_aa": "<string>"
    },
    "message": "<string>"
  },
  "status": "<string>"
}
```

## 1. Позитивные тесты

| №     | Описание                                                                  | Параметры запроса                              | Ожидаемый результат                                                                                                                                                                                                                                                                                                                                                                      |
|:------|:--------------------------------------------------------------------------|:-----------------------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1** | Сформировать GET-запрос с path-параметром - допустимым значением sellerID | sellerID = "sellerID_из_созданного_объявления" | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 1**. Значения полей тела ответа соответствуют значениям полей из тела запроса созданного объявления. Если пользователем было создано несколько объявлений, то в теле ответа весь список созданных объявлений. Если пользователь не создавал объявления или удалял их, то в теле ответа пустой список. |

## 2. Негативные тесты
| №     | Описание                                                                           | Параметры запроса                                                 | Ожидаемый результат                                                                                                                                                                                                                                                            |
|:------|:-----------------------------------------------------------------------------------|:------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **2** | Сформировать GET-запрос с path-параметром - типом, не соответствующий спецификации | sellerID = "123abc" (не соответствует формату числового значения) | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке недопустимого значения `sellerId`. |

## 3. Граничные тесты
| №     | Описание                                                                   | Параметры запроса   | Ожидаемое тело ответа (пример)                                                                                                                                                                                                                                                                                                                                                                                |
|:------|:---------------------------------------------------------------------------|:--------------------|:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **3** | Сформировать GET-запрос с path-параметром - значением `sellerID = 111111`  | sellerID = "111111" | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 1**. Значения полей тела ответа соответствуют значениям полей из тела запроса созданного объявления данным пользователем. Если пользователем было создано несколько объявлений, то в теле ответа весь список созданных объявлений. Если пользователь не создавал объявления или удалял их, то в теле ответа пустой список. |
| **4** | Сформировать GET-запрос с path-параметром - значением `sellerID = 999999`  | sellerID = "999999" | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 1**. Значения полей тела ответа соответствуют значениям полей из тела запроса созданного объявления данным пользователем. Если пользователем было создано несколько объявлений, то в теле ответа весь список созданных объявлений. Если пользователь не создавал объявления или удалял их, то в теле ответа пустой список. |
| **5** | Сформировать GET-запрос с path-параметром - значением `sellerID = 111110`  | sellerID = "111110" | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке допустимого значения sellerId.                                                                                                                                    |
| **6** | Сформировать GET-запрос с path-параметром - значением `sellerID = 1000000` | sellerID = "1000000" | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 3**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке допустимого значения sellerId.                                                                                                                                    |


# Тест-кейсы для метода GET {{baseUrl}}/api/2/statistic/:id (Получить статистику по объявлению)

## Предусловия

1. При помощи метода POST {{baseUrl}}/api/2/item создать объявление
2. Из тела ответа взять значение поля `id`
3. Использовать значение в path-параметре

- **Эндпоинт:** `{{baseUrl}}/api/2/statistic/:id`
- **Метод:** `GET`
- **Заголовки:** `Accept: application/json`

**Схема 1:** Схема успешного ответа 200 OK для GET {{baseUrl}}/api/2/statistic/:id.

**Схема 2:** Схема ответа об ошибке 404 Not Found.

**Схема 1:**

``` 
[
  {
    "likes": "<integer>",
    "viewCount": "<integer>",
    "contacts": "<integer>"
  },
  {
    "likes": "<integer>",
    "viewCount": "<integer>",
    "contacts": "<integer>"
  }
]
```

**Схема 2:**

``` 
{
  "result": "laborum",
  "status": "cillum enim eiusmod"
}
```

## 1. Позитивные тесты

| №     | Описание                                                                             | Параметры запроса                                      | Ожидаемый результат                                                                                                                                                                     |
|:------|:-------------------------------------------------------------------------------------|:-------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1** | Сформировать GET-запрос с path-параметром - корректным и существующим в системе UUID | id = "id_из_созданного_объявления" (существующий uuid) | Сервер возвращает HTTP статус-код `200 OK`. Структура ответа соответствует **схеме 1**. Значения полей тела ответа соответствуют значениям полей из тела запроса созданного объявления. |

## 2. Негативные тесты
| №     | Описание                                                                                           | Параметры запроса                                                 | Ожидаемый результат                                                                                                                                                                                                          |
|:------|:---------------------------------------------------------------------------------------------------|:------------------------------------------------------------------|:-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **2** | Сформировать GET-запрос с path-параметром - валидным по формату, но не существующим в системе UUID | id = "1d0321d3-dd36-4baf-98f1-fdcd37266db5" (несуществующий uuid) | Сервер возвращает HTTP статус-код `404 Not Found`. Структура ответа соответствует **схеме 2**. Поле `status` содержит возвращаемый код-статус, поле `result` - сообщение об ошибке несуществования объявления с данным UUID. |
| **3** | Сформировать GET-запрос с path-параметром - не соответствующим формату UUID                        | id = "123abc" (не соответствует формату uuid)                     | Сервер возвращает HTTP статус-код `404 Not Found`. Структура ответа соответствует **схеме 2**. Поле `status` содержит возвращаемый код-статус, поле `result` - сообщение об ошибке недопустимого значения `id`.                |


# Тест-кейсы для метода DELETE {{baseUrl}}/api/2/item/:id (Удалить объявление по идентификатору)

## Предусловия

1. При помощи метода POST {{baseUrl}}/api/1/item создать объявление
2. Из тела ответа взять значение поля `id`
3. Использовать значение в path-параметре

- **Эндпоинт:** `{{baseUrl}}/api/2/item/:id`
- **Метод:** `DELETE`
- **Заголовки:** `Accept: application/json`

**Схема 1:** Схема ответа об ошибке 404 Not Found.

**Схема 2:** Схема ответа об ошибке 400 Bad Request.

**Схема 1:**

``` 
{
  "result": "laborum",
  "status": "cillum enim eiusmod"
}
```

**Схема 2:**

``` 
{
  "result": {
    "messages": {
      "culpa_b92": "<string>",
      "enim_24f": "<string>",
      "mollit_aa": "<string>"
    },
    "message": "<string>"
  },
  "status": "<string>"
}
```


## 1. Позитивные тесты

| №     | Описание                                                                                | Параметры запроса                                      | Ожидаемый результат                                             |
|:------|:----------------------------------------------------------------------------------------|:-------------------------------------------------------|:----------------------------------------------------------------|
| **1** | Сформировать DELETE-запрос с path-параметром - корректным и существующим в системе UUID | id = "id_из_созданного_объявления" (существующий uuid) | Сервер возвращает HTTP статус-код `200 OK`. Пустое тело ответа. |

## 2. Негативные тесты
| №     | Описание                                                                                              | Параметры запроса                                                 | Ожидаемый результат                                                                                                                                                                                                                                                      |
|:------|:------------------------------------------------------------------------------------------------------|:------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **2** | Сформировать DELETE-запрос с path-параметром - валидным по формату, но не существующим в системе UUID | id = "1d0321d3-dd36-4baf-98f1-fdcd37266db5" (несуществующий uuid) | Сервер возвращает HTTP статус-код `404 Not Found`. Структура ответа соответствует **схеме 1**. Поле `status` содержит возвращаемый код-статус, поле `result` - сообщение об ошибке несуществования объявления с данным UUID.                                             |
| **3** | Сформировать DELETE-запрос с path-параметром - не соответствующим формату UUID                        | id = "123abc" (не соответствует формату uuid)                     | Сервер возвращает HTTP статус-код `400 Bad Request`. Структура ответа соответствует **схеме 2**. Поле `status` содержит возвращаемый код-статус, поле `message` - общую информацию о виде/типе ошибки, блок `message` - сообщение об ошибке недопустимого значения `id`. |
